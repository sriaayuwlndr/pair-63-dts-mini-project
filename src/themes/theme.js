import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
      primary: {
        main: '#97dab7',
        dark: '#49715a',
      },
      secondary: {
        main: '#272325',
        light: '#d4d4d4',
      },
    },
});

export default theme;